extends layout

block content
    content
        if username 
            h4= username 
        form(action="/helo", method="POST")
            input(type="text", name="username", placeholder="silakan isi")
            input(type="submit" value="Submit")